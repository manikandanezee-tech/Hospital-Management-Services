-- CHECK FOR GIVEN INPUT IS NULL
DELIMITER //

	CREATE FUNCTION EZEE_FN_ISNULL(pcrString VARCHAR(100))	

	RETURNS TINYINT

	DETERMINISTIC

	RETURN IF(ISNULL(pcrString)OR TRIM(pcrString)='',TRUE,FALSE);

	//
DELIMITER ;